{
  "name": "soroban-machine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Soroban / abacus skill trainer. Runtime is dependency-free static HTML/CSS/JS; this package.json only carries dev tooling for the e2e test suite.",
  "scripts": {
    "test": "node tests/smoke.mjs",
    "serve": "node scripts/serve.mjs",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
